
"""进程和线程的概述"""

"""
    进程就是运行中的应用程序。应用程序是静态的，进程是动态的。
    查看所有正在运行的进程，每个进程至少对应一个线程。每个进程都用来完成一件任务，
每个线程用来完成进程内的一件子任务。线程就是进程内部的一个执行单元。

    每个进程都有独立的内存空间，而进程内的所有线程共享内存空间。
"""

"""
    不管是单核CPU还是多核CPU都是可以支持多任务的。
    多个任务交替执行方式称为并发
    多个任务同时执行的方式称为并行。
    
"""
